node_modules/
dist/

# Environment files — never commit. Vite inlines these at build time, so a
# committed .env leaks straight into the shipped bundle. Document the required
# variables in .env.example instead.
.env
.env.*
!.env.example

.DS_Store
*.log
